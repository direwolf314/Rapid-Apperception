=======
Tagger
=======
TODO:
Context awareness 
    - if we see #include <sql.h> then we can assume that some string is a db op


=====
UI
=====
TODO:
Restore the state of the file tree whenever it refreshes
'Open project' button
    - Have a list of projects (root folders) in database
    - If selected folder (project doesn't exist) then trigger tagger
'n/N' key bindings to go jump forward/back in matches
'enter' key binding on add_tag
